spring:
  cloud:
    nacos:
      config:
        server-addr: #服务器地址nacos地址
        file-extension: yaml
        namespace: #命名空间
      discovery:
        server-addr: #服务器地址nacos地址
  application:
    name: service-msm
